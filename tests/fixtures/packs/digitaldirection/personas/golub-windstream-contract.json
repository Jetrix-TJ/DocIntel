{
  "sender_fingerprint": "digitaldirection|windstream",
  "doc_type": "contract",
  "rule_version": "v1",
  "status": "active",
  "notes": "Windstream/Golub contracts, amendments and renewals - a real, curated fixture set (docs/corpus/contracts/), not synthetic. Layouts vary more between a base Service Agreement and its amendments than between two invoices of the same template, so this persona starts narrow (the two fields every real document in the set actually prints in a findable way) rather than guessing at a full field set no document has confirmed.",
  "field_selectors": [
    {
      "field": "contract_number",
      "anchor": "Quote #:",
      "anchor_alts": ["Quote#", "Quote #"],
      "anchor_occurrence": "last",
      "region": "near-anchor",
      "pattern": "\\d{5,8}",
      "required": false,
      "adjust": ["resolve_vendor_alias", "resolve_bill_to_alias"]
    },
    {
      "field": "effective_date",
      "anchor": "Effective Date",
      "region": "near-anchor",
      "pattern": "date",
      "required": false,
      "adjust": ["normalize_date_iso"]
    }
  ]
}
